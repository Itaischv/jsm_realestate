import { Box } from "@chakra-ui/react";

const Footer = () => {
    return(
        <Box textAlign={`center`} p={`5`} color={`gray.600`}>
            Real Estate, 2021.
        </Box>
    )
}

export default Footer;